# Decision records

Short records of measured routing decisions. Each file holds the context, the
measurement, the decision, and the constraint code must keep enforcing.

In-code comments state the constraint and point here. Incident narratives,
row counts, dates, and reviewer labels live in these files, not beside the
code they once justified.
